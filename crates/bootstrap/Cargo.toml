[package]
name = "bevy_bootstrap"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy_app = { version = "0.15.2" }
bevy_ecs = { version = "0.15.2", features = ["serialize"] }
bevy_bootstrap_macros = { path = "macros" }
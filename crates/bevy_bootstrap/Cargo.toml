[package]
name = "bevy_bootstrap"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["rlib", "dylib"]

[dependencies]
bevy_app = { git = "https://github.com/gehud/bevy.git", branch = "dynamic" }
bevy_bootstrap_macros = { path = "macros" }